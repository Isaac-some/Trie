services:
  - type: web
    name: trie-transfer-service
    runtime: docker
    healthCheckPath: /api/health
    envVars:
      - key: DATA_DIR
        value: /tmp/trie-transfer-data
      - key: SESSION_TTL_MS
        value: '3600000'
